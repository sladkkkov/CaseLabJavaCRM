server:
  port: 8080

spring:
  application:
    name: CaseLab

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQL94Dialect
    generate-ddl: true

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://89.223.124.249:5432/
    name: caselab
    username: case_lab_admin
    password: 8b,4fA]]’o

  profiles:
    active: @profileActive@

